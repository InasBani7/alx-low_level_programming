#include<stdio>
/*
 * main - Entry Point
 * Description: detects if n is -ve or +ve and prints out a message accoringly
 * Return: 0 (success)
 */
int main(void)
{
	int n;

	if (n < o)
		fprint("is negative");
	else if (n > 0)
		fprint("is negative");
	else
		fprint("is zero");
	return (0);
}
