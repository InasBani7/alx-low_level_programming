#include <unistd.h>
#include "main.h"

/**
 * _putchar - Prints out it's argument 'a' to the standard output
 * @a: The argument or type char
 * Return: 1 (Success) , -1 (error)
 */
int _putchar(char a)
{
	return (write(1, &c, 1));
}
