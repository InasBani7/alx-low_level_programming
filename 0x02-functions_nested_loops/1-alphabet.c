#include<main.h>

/**
 * print_alphabet - using the user defind function _putchar
 * to print lowercase alphabets
 */
void print_alphabet(void)
{
	int alpha;

	for (alpha = 'a'; alpha <= 'z' ; alpha++)
		_putchar(ch);
	_putchar('\n');
}
